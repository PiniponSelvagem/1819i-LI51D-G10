package pt.isel.pdm.i41n.g6.yama.activities.userdetails

import android.support.v7.app.AppCompatActivity
import android.os.Bundle
import kotlinx.android.synthetic.main.activity_user.*

class UserDetailsActivity : AppCompatActivity() {

    /*
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_user)

        user_avatar.setOnClickListener {
            avatarClicked()
        }
    }



    /**
     * "You are an NPC!"
     */
    private val activateNPC = 8
    private var activateCount = 0
    private var youAreNPC = false
    private fun avatarClicked() {
        if (!youAreNPC) {
            if (activateCount == activateNPC)
                user_avatar.setImageResource(R.drawable.avatar_npc)
            else
                ++activateCount
        }
    }
    */
}
