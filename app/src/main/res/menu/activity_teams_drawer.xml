<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group
        android:id="@+id/top"
        android:checkableBehavior="single">
        <item
            android:id="@+id/item1"
            android:title="Item1" /> <!-- android:icon="@drawable/ic_menu_camera" -->
    </group>

    <group
        android:id="@+id/middle"
        android:checkableBehavior="single">
        <item
            android:id="@+id/item2"
            android:title="Item2" /> <!-- android:icon="@drawable/ic_menu_share" -->
    </group>

    <group
        android:id="@+id/bottom"
        android:checkableBehavior="single">
        <item
            android:id="@+id/item3"
            android:title="Item3" /> <!-- android:icon="@drawable/ic_menu_about" -->
    </group>

</menu>